import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h2>Context API</h2>
            <p>For the React app, Context API is the way of producing global variable which can be used by any component. It is a solution of props drilling problem. By this, we do not need to pass variable through grand parents, parent, etc.</p>
            <hr></hr><hr></hr>
            <h2>Semantic Tag</h2>
            <p>Semantic tag makes a html script more clear and meaningful. Developers use section, main, article tags insteed of div tags. They seperate a web page into different parts and then use meaningful tags.</p>
        </div>
    );
};

export default Blogs;